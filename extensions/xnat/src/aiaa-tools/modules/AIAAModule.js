
const state = {
  menuIsOpen: false,
};

const configuration = {};

const api = {
  client: undefined,
  isConnected: false,
  isSuccess: true,
  models: [],
};

const getters = {};

const setters = {};

export default {
  state,
  getters,
  setters,
  configuration,
  api,
};